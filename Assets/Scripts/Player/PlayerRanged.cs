using UnityEngine;

namespace PlayerSystems
{
    public class PlayerRanged : Core.Player
    {
        public override async void Move(Vector2 newPos)
        {
            // check for attack range
            
                 // attack if it ok
            
                    
            // move
            
            
            base.Move(newPos);
        }
    }
}