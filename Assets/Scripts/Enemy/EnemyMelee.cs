using UnityEngine;

namespace Enemy
{
    class EnemyMelee : Core.Enemy
    {
        public override async  void Move(Vector2 newPos)
        {
            // checkDistance   
            
            base.Move(newPos);
        }
    }
}