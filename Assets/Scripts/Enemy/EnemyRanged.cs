using UnityEngine;

namespace Enemy
{
    class EnemyRanged : Core.Enemy
    {
        public override async  void Move(Vector2 newPos)
        {
            // check distance 
            
            
            base.Move(newPos);
        }
    }
}